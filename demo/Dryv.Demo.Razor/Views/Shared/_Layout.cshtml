<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dryv Demo - Razor Views</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css" />
    <style>
        .field-validation-error { color: #e74c3c; font-size: 0.85em; display: block; margin-top: 0.25em; }
        .input-validation-error { border-color: #e74c3c; }
        form > div { margin-bottom: 1rem; }
    </style>
</head>
<body>
    <h1>Dryv Demo &mdash; ASP.NET MVC + Razor Views</h1>
    @RenderBody()
    @RenderSection("Scripts", required: false)
</body>
</html>
